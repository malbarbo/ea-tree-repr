\documentclass[tikz]{standalone}
\usepackage[T1]{fontenc}
\usepackage[brazil]{babel}
\usepackage{pgfplots}
\pgfplotsset{compat=1.13}
\usetikzlibrary{calc}

\begin{document}
  \begin{tikzpicture}
    \begin{axis}[
        name=plt,
        legend pos = north west,
        xlabel=Iteration,
        ylabel=Subtree length,
        legend to name={leg},
        legend style={legend columns=2}
      ]
      \addplot+ [mark=none] table {../results/nddr-subtree-len-1/adj};
      \addplot+ [mark=none] table {../results/nddr-subtree-len-1/free};
      \legend{Adjacent, Free}
    \end{axis}
    \node[yshift=-1.5cm] at (plt.south) {\ref{leg}};
  \end{tikzpicture}
\end{document}
